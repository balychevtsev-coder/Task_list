# 🚀 Быстрая настройка бота

## Шаг 1: Получение токена бота

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: `My Task Bot`)
4. Введите username бота (например: `my_task_bot` - должен заканчиваться на `bot`)
5. Скопируйте полученный токен (выглядит примерно так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Создание файла .env

Создайте файл `.env` в корне проекта `telegram_task_bot/` со следующим содержимым:

```
BOT_TOKEN=ваш_токен_сюда
```

Например:
```
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

## Шаг 3: Установка зависимостей

Откройте терминал в папке проекта и выполните:

```bash
pip install -r requirements.txt
```

## Шаг 4: Запуск бота

```bash
python main.py
```

Если всё настроено правильно, вы увидите:
```
🚀 Запуск бота...
✅ Все обработчики зарегистрированы
📡 Бот начал получать обновления
```

## Шаг 5: Тестирование

1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте `/start`
4. Попробуйте команды:
   - `/add Тестовая задача`
   - `/list`
   - `/list_csv`

## ⚠️ Важно!

- **НЕ публикуйте токен бота в открытом доступе!**
- Файл `.env` добавлен в `.gitignore` и не будет загружен в Git
- Храните токен в безопасности

## 🔧 Устранение неполадок

### Ошибка: "Unauthorized"
- Проверьте правильность токена
- Убедитесь, что в файле `.env` нет пробелов вокруг знака `=`
- Токен должен быть без кавычек

### Ошибка: "No module named 'aiogram'"
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### Ошибка: "No such file or directory: '.env'"
- Создайте файл `.env` вручную
- Убедитесь, что файл находится в папке `telegram_task_bot/`

---

Готово! Бот настроен и готов к работе! 🎉

